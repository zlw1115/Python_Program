'''
字符串、数字、列表、元组、字典、空类型与布尔值的关系总结

每一种数据类型，自身的值都有表示True和False
not对于一切结果取反

数据类型        为True           为false
int            非0               0
float          非0.0             0.0
str             len(str)!=0     len(str) == 0 即‘’
list            len(str)!=0     len(str) == 0 即[]
tuple           len(str)!=0     len(str) == 0 即()
dict            len(str)!=0     len(str) == 0 即{}
None            not None        None
'''
